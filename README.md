# Jarvis

This is a multimodal assistant application which unlike the traditional Agentic AI modals accepts the user's input in format, converts the input into a set of well defined instructions, uses various tools to accompolish the tasks and finally addresses the user after successful execution.
